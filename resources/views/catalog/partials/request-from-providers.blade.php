{{-- Right column content; card wrapper is in parent. --}}
<h2 class="h5 text-body mb-3">{{ __('catalog.request_from_providers_heading') }}</h2>
<button
    type="button"
    class="btn btn-outline-secondary"
    disabled
    title="{{ __('catalog.request_from_providers_title') }}"
>
    {{ __('catalog.request_from_providers') }}
</button>
<p class="text-muted small mt-2 mb-0">{{ __('catalog.request_from_providers_hint') }}</p>
